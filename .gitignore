# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environments
venv/
env/
ENV/
.venv/
venv_build/              # 打包用的虚拟环境

# Poetry
poetry.lock
# 注意：poetry.lock 通常应该提交，但如果团队不使用 Poetry 可以不提交

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Streamlit
.streamlit/

# ChromaDB
chroma_db/
*.db
*.sqlite

# HuggingFace 模型文件（手动下载的模型）
models/
*.bin
*.safetensors
*.pt
*.pth
*.onnx
*.h5
*.ckpt

# HuggingFace 缓存（可选，通常不需要提交）
# .cache/huggingface/

# API 密钥配置文件
.deepseek_config.json
*.config.json

# Prompt 模版文件（用户自定义的模版）
prompt_templates/

# 保存的报告和问答记录（用户生成的内容）
saved_reports/
saved_qa/

# 环境变量
.env
.env.local

# 操作系统
.DS_Store
Thumbs.db
desktop.ini

# 日志
*.log

# 临时文件
*.tmp
*.temp

# PyInstaller打包相关文件
# build/ 和 dist/ 已在上面定义
*.spec                    # PyInstaller spec文件（如果生成在根目录，build_script目录中的应该提交）
*.manifest                # Windows清单文件
*.pyd                     # Python扩展模块（打包时生成）

