# 基础 Web 框架
streamlit>=1.28.0

# 文档处理
python-docx>=1.1.0
pypdf>=3.17.0
pandas>=2.0.0
openpyxl>=3.1.0

# LangChain 相关
# 注意：langchain-community 0.3.23+ 要求 NumPy 2.1.0+，与 ChromaDB 冲突
# 使用 0.3.22 或更早版本以兼容 NumPy 1.x
langchain>=0.1.0
langchain-community>=0.0.20,<0.3.23
langchain-core>=0.1.0
langchain-text-splitters>=0.2.0
# 新的独立包（推荐使用，避免弃用警告）
langchain-huggingface>=0.0.1
langchain-chroma>=0.1.0
# langsmith 版本限制（langchain-community 0.3.22 要求 < 0.4）
langsmith>=0.1.125,<0.4

# 向量数据库（如果安装失败，请参考 INSTALL.md）
# 注意：chromadb 依赖 zstandard，可能需要编译工具
# NumPy 版本限制（ChromaDB 不兼容 NumPy 2.0）
numpy>=1.24.0,<2.0.0
chromadb>=0.4.22,<0.5.0

# AI 模型相关
sentence-transformers>=2.2.0
requests>=2.31.0
torch>=2.0.0
transformers>=4.35.0

# 联网搜索功能（可选，用于智能问答的联网搜索）
# 注意：duckduckgo-search 已重命名为 ddgs，优先使用新库
ddgs>=9.0.0
# 如果新库不可用，可以回退到旧库（会显示警告）
# duckduckgo-search>=6.0.0

